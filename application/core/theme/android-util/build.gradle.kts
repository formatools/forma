// TODO split DI and resources
androidUtil(
    packageName = "com.stepango.blockme.core.theme.android.util",

    dependencies = deps(
        androidx.appcompat,
        google.material,
        google.dagger
    )
)