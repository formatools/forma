# v3 Starlark call site — attributes only.
# gradlePath equivalent: :feature:calculator:api  type=jvm.api

load("//forma:defs.bzl", "jvm_api")

jvm_api(
    name = "api",
    visibility = [
        "//binary:__pkg__",
        "//feature/calculator/impl:__pkg__",
    ],
)
